1.Print each character in newline from given string********
package dinesh21;

import java.util.Scanner;

public class dinesh21 {
    public static void main(String[] args) {
        Scanner dk =new Scanner (System.in);
        System.out.println("Enter the number:");
        String name = dk.nextLine();
        for (int i =0;i < name.length();i++)
        {
            System.out.println(name.charAt(i));
        }
    }
}
OUTPUT:
Enter the number:
dinesh 
d
i
n
e
s
h

2.Print ASCII value for each character in given string*******
package dinesh21;

public class ASCII {
    public static void main(String[] args) {
        String name = "dinesh kumar";
        for(int i =0;i<name.length();i++) {


            int ascii = name.charAt(i);
            System.out.println(ascii);
        }
    }
}
OUTPUT:
100
105
110
101
115
104
32
107
117
109
97
114
3.Print only UPPERCASE of character in given string**

package dinesh21;

public class uppercase {
    public static void main(String[] args) {
        String name = "Dinesh Kumar";
        for(int i=0;i<name.length();i++) {
            char ch  = name.charAt(i);
            if(Character.isUpperCase(ch))
            {
                System.out.println(ch);
            }



        }
    }
}
OUTPUT:
D
K
4.Print only lowercase of character in given  string***
package dinesh21;

public class lowercase {
    public static void main(String[] args) {
        String name ="Dinesh Kumar";
        for(int i =0;i<name.length();i++)
        {
            char ch = name.charAt(i);
            if(Character.isLowerCase(ch))
            {
                System.out.println(ch);
            }
        }
    }
}
OUTPUT:
i
n
e
s
h
u
m
a
r

5.print Alphabet count in given string*******
package dinesh21;

public class alphabetcount {
    public static void main(String[] args) {
        String name = "dinesh kumar";
        int count=0;
        for(int i =0;i<name.length();i++)
        {
            char ch = name.charAt(i);
            if(Character.isLetter(ch))
            {
                count++;
            }
        }
        System.out.println(count);
    }
}
OUTPUT:
11

6.Print Count of UPPER & lower case of given String***
package dinesh21;

public class upperandlowercase {
    public static void main(String[] args) {
        String name =" DiNesh KuMar";
        int count =0;
        int digit=0;
        for(int i=0;i<name.length(); i++)
        {
            char ch = name.charAt(i);
            if(Character.isUpperCase(ch))
            {
                count++;
            }
            else if(Character.isLowerCase(ch))
            {
                digit++;
            }
        }
        System.out.println("uppercase:"+count);
        System.out.println("lowercase:"+digit);


    }
}
OUTPUT:
uppercase:4
lowercase:7

7.Create a new string containing only UPPERCASE from given string***
package dinesh21;

public class onlyUppercaseString {
    public static void main(String[] args) {
        String name =" DiNeSH kumAr";
        for(int i =0;i<name.length();i++)
        {
            char ch = name.charAt(i);
            if(Character.isUpperCase(ch))
            {
                System.out.print("uppercase letter only:"+ch);
            }
        }
    }
}
OUTPUT:
DNSHA

8.Create a new string with digits followed by letters from given string
package dinesh21;

import java.util.Scanner;

public class StringwithLetter {
    public static void main(String[] args) {
        String  name = "ab1cd234ghtj";
        System.out.println("before rearrange the string :"+name);
        StringBuilder digits = new StringBuilder();
        StringBuilder letters = new StringBuilder();

        for(int i=0;i< name.length();i++)
        {
            char ch =name.charAt(i);
            if(Character.isDigit(ch))
            {
                digits.append(ch);
            }
            else if(Character.isLetter(ch))
            {
                letters.append(ch);
            }
        }
        String result = digits.toString()+letters.toString();
        System.out.println(" rearranged string:"+result );
    }
}
OUTPUT:
before rearrange the string :ab1cd234ghtj
 rearranged string:1234abcdghtj

9.Create a new string after removing all spaces in given string

package dinesh21;

public class RemovingAllSpace {
    public static void main(String[] args) {
        String letter ="dinesh kumar";
        StringBuilder name = new StringBuilder();
        for(int i =0;i<letter.length();i++)
        {
           char  ch = letter.charAt(i);
            if(!Character.isSpaceChar(ch))   // ! logical NOT IF NOT SPACE CONDITION TRUE
            {
               name.append(ch);
            }
        }
        System.out.println(name.toString());

    }
}
OUTPUT:
dineshkumar

11.check given string is Palindrome or not

package dinesh21;

import com.sun.security.jgss.GSSUtil;

import java.util.Scanner;

public class palindromeString {
    public static void main(String[] args) {
        Scanner dk= new Scanner (System.in);
        System.out.println(" Enter the string:");
        String original = dk.nextLine();
         String reverse = "";
        for(int i=original.length()-1;i>=0;i--)
        {
         reverse =reverse + original.charAt(i);
        }
        if(original.equals(reverse))
        {
            System.out.println("yes .. its a palindrome string:");
        }
        else
        {
            System.out.println(" its not a palindrome string:");
        }

    }
}
OUTPUT:
 Enter the string:
madam
yes .. its a palindrome string:

12.Create a word encoding program which moves each letter by 2
Example A becomes C,B becomes D,Y becomes a so on.,

package dinesh21;

public class EncodingMovesLetter {
    public static void main(String[] args) {
        String input = "HelloWorLd 123";
        String result;
        StringBuilder encoder = new StringBuilder();
        for(int i =0;i<input.length();i++)
        {
            char ch = input.charAt(i);
            if(Character.isUpperCase(ch))
            {
                char shifted = (char)  (((ch-'A'+2)%26)+'A');
                encoder.append(shifted);
            } else if (Character.isLowerCase(ch))
            {
                char shifted =(char)(((ch-'a'+2)%26)+'a');
                encoder.append(shifted);

            }
        }
       result = encoder.toString();
        System.out.println(" original: "+input);
        System.out.println(" Encoder: "+result);

    }
}

output:
 original: HelloWorLd 123
 Encoder: JgnnqYqtNf


13.Print Each word in separate line from the given sentence


package dinesh21;

import java.util.Scanner;

public class SeperatedLIneSentence {
    public static void main(String[] args) {
        Scanner dk = new Scanner(System.in);
        System.out.println("Enter the number:");
        String name = dk.nextLine();
        for(int i=0;i<name.length();i++)
            {
              char ch = name.charAt(i);
            if(ch==' ')
            {
                System.out.println();
            }
            else {
                System.out.print(ch);
            }
        }
       // System.out.println();
    }
}
output:
Enter the number:
dinesh kumar
dinesh
kumar

14.print the first letter of each word in a sentence



